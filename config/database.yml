# config/database.yml
default: &default
  adapter: postgresql
  encoding: unicode
  url: <%= ENV.fetch("DATABASE_URL", "") %>
  pool: 10

development:
  <<: *default

test:
  <<: *default

production:
  <<: *default
