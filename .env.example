# Example environment variables for local development
RAILS_ENV=development
PORT=3000
REDIS_URL=redis://localhost:6379/0
WEB_CONCURRENCY=0
RAILS_MAX_THREADS=5
SIDEKIQ_CONCURRENCY=5

# Supabase DATABASE_URL examples:
# Use the pooled connection (recommended for apps) — requires project-specific username (postgres.<project-ref>)
# DATABASE_URL=postgresql://postgres.<your-project-ref>:<your_password>@aws-1-<region>.pooler.supabase.com:6543/postgres?sslmode=require
# PREPARED_STATEMENTS=false  # required for pgBouncer pooler

# Or use direct connection (no pooler) for migrations or scripts
# DATABASE_URL=postgresql://postgres:<your_password>@aws-1-<region>.supabase.com:5432/postgres?sslmode=require
# PREPARED_STATEMENTS=true   # direct Postgres supports prepared statements

# Embeddings provider
# EMBEDDING_PROVIDER=dummy   # default for dev (no external calls)
# OPENAI_API_KEY=sk-...
# OPENAI_EMBEDDING_MODEL=text-embedding-3-small
# DEMO_USER_EMAIL=dev@local.test

# --- Google OAuth ---
# Obtain from Google Cloud Console → Credentials → OAuth 2.0 Client IDs
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# Dev redirect URI to add in Google Console (must match EXACTLY):
#   http://localhost:3000/auth/google/callback
# If you use a different host/port (127.0.0.1:3000, 0.0.0.0:3000, ngrok, etc),
# set GOOGLE_REDIRECT_URI to that exact URL and also add it to the Google Console.
GOOGLE_REDIRECT_URI=
